on:
  pull_request:
  workflow_dispatch: # Allow manual triggering

# Remember to change TAK_RELEASE also in push_to_main.yml
env:
  TAK_RELEASE: 5.8-RELEASE-69

jobs:
  pre-commit:
    runs-on: ubuntu-latest
    name: Pre-commit
    steps:
      - uses: actions/checkout@v6
      - uses: pvarki/config-ci-library/.github/actions/setup-python@main
        with:
          python-version: "3.11"
      - uses: pvarki/config-ci-library/.github/actions/setup-typescript@main
        with:
          working-directory: ui
          node-version: "24"
          pnpm-version: "11"
      - name: Run pre-commit checks
        run: |
          uv run docker/pre_commit_init.sh

  bumpversion-check:
    runs-on: ubuntu-latest
    name: Validate .bumpversion.toml
    steps:
      - uses: actions/checkout@v6
      - uses: pvarki/config-ci-library/.github/actions/validate-bumpversion@main

  test:
    runs-on: arc-runner-set
    permissions:
      contents: write
      pull-requests: write
      checks: write
    steps:
      - uses: actions/checkout@v5
        with:
          submodules: recursive
      - name: Build test image
        run: |
          docker build --target test -t takrmapi:test .
      - name: run test image
        run: |
          docker run --rm takrmapi:test
      - name: Publish Test Report
        uses: mikepenz/action-junit-report@v4
        if: success() || failure()
        with:
          report_paths: "**/pytest*.xml"
          detailed_summary: true
          check_name: "JUnit report (local)"

  snyk-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: pvarki/config-ci-library/.github/actions/setup-python@main
        with:
          python-version: "3.11"
      - uses: pvarki/config-ci-library/.github/actions/snyk-test@main
        with:
          snyk-org: deployapp-products
          snyk-token: ${{ secrets.SNYK_TOKEN }}

  docker_build_test:
    name: Test Docker image builds
    runs-on: arc-runner-set
    steps:
      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ vars.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - uses: actions/checkout@v6
        with:
          submodules: recursive
      - name: Build production target
        run: docker build --target production -t takrmapi:production .

  publish:
    name: Publish Docker image
    runs-on: arc-runner-set
    needs: [pre-commit, bumpversion-check, test, docker_build_test]
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v6
        with:
          submodules: recursive
      - name: Publish Docker image
        uses: pvarki/config-ci-library/.github/actions/publish-image@main
        with:
          product: tak
          component: integration
          extra-tag: tak${{ env.TAK_RELEASE }}
          build-args: |
            TAKSERVER_IMAGE=ghcr.io/pvarki/tak-server:${{ env.TAK_RELEASE }}
          dockerhub-username: ${{ vars.DOCKERHUB_USERNAME }}
          dockerhub-token: ${{ secrets.DOCKERHUB_TOKEN }}
          github-token: ${{ secrets.GITHUB_TOKEN }}
          acr-repo: ${{ vars.ACR_REPO }}
          acr-username: ${{ vars.ACR_USERNAME }}
          acr-token: ${{ secrets.ACR_TOKEN }}
